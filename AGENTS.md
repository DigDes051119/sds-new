# Workspace AI Agent Rules

- **Token Optimization (RTK)**: Always use `rtk` (Rust Token Killer at `C:\Users\Akimkhan\rtk\rtk.exe` or `rtk`) prefix when running terminal/CLI commands (`rtk git ...`, `rtk npx vite build`, `rtk read ...`, `rtk grep ...`, etc.) to filter outputs and maximize token efficiency.
